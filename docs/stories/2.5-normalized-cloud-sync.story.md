# Story 2.5 — Sincronização normalizada sem regressão visual

**Status:** Done  
**Epic:** 2 — Biblioteca e persistência  
**Origem:** `docs/briefing.md`, FR-01/FR-05/FR-09, NFR-03/NFR-04/NFR-08

## História

Como colaborador da equipe, quero que o modo nuvem use páginas e blocos normalizados quando disponíveis, para manter sincronização multi-dispositivo sem quebrar catálogos legados.

## Critérios de aceite

- [x] A leitura consulta `catalog_pages`/`page_sections` e valida blocos antes de entregar o estado ao editor.
- [x] Catálogos sem a migration ou sem páginas normalizadas continuam usando `brand.pages`/modo local.
- [x] A gravação mantém o bundle legado e tenta persistir a composição normalizada.
- [x] Falha na tabela nova não impede o fallback e fica registrada no console.
- [x] Nenhum componente visual ou layout do baseline é alterado.

## File list

- `lib/supabase/api.ts`
- `docs/stories/2.5-normalized-cloud-sync.story.md`

## Dev Agent Record

### Completion notes

O caminho Supabase agora faz uma migração gradual: consulta o modelo relacional quando ele existe, mantém `brand.pages` como fallback de compatibilidade e grava nos dois formatos durante a transição. O editor visual continua recebendo o mesmo `CatalogPage[]`.

### Validation

`npm test` passou com 26 casos; `npm run typecheck` e `npm run lint -- --quiet` passaram. O build de produção foi executado com a configuração de um worker exigida pelo sandbox local.
