# Story 1.1 — Fundação AIOX e baseline visual

**Status:** Done  
**Epic:** 1 — Fundação do produto  
**Origem:** `docs/briefing.md`, FR-01/FR-09/FR-10, NFR-01/NFR-06

## História

Como responsável pelo produto, quero uma linha de reconstrução orientada pelo AIOX Core e baseada no editor antigo, para que o time possa evoluir o domínio sem perder o design aprovado nem a rastreabilidade.

## Critérios de aceite

- [x] A branch de reconstrução parte do commit visual antigo e não altera o diretório original.
- [x] `.aiox-core/`, `core-config.yaml`, Constitution, skills e comandos locais estão disponíveis no projeto.
- [x] `docs/briefing.md`, `docs/prd.md`, `docs/architecture.md` e `docs/frontend/frontend-spec.md` registram requisitos confirmados, restrições e decisões pendentes.
- [x] O app antigo abre com a mesma estrutura visual antes da primeira migração funcional.
- [x] Um health check/diagnóstico CLI identifica stack, configuração e dependências sem depender da UI.
- [x] `npm run lint`, `npm run typecheck`, `npm test` e `npm run build` têm resultado registrado; limitações do ambiente ficam documentadas.

## Notas de implementação

- Não copiar a implementação intermediária para esta branch.
- Usar aliases `@/` em novos módulos.
- Não conectar o Supabase nem alterar schema nesta story; isso entra na story de persistência após revisão de dados.

## File list

- `.aiox-core/`
- `.codex/skills/aiox-*`
- `.aiox-core/core-config.yaml`
- `eslint.config.mjs`
- `package.json`
- `scripts/health-check.mjs`
- `package.json`
- `docs/briefing.md`
- `docs/prd.md`
- `docs/architecture.md`
- `docs/architecture/system-architecture.md`
- `docs/frontend/frontend-spec.md`
- `supabase/docs/SCHEMA.md`
- `supabase/docs/DB-AUDIT.md`
- `docs/stories/1.1-fundacao-baseline.story.md`

## Dev Agent Record

### Completion notes

A worktree limpa foi criada em `catalog-builder-rebuild`, baseada no `HEAD` visual antigo, sem alterar o diretório original. O AIOX Core e as projeções Codex foram incorporados sem `node_modules`. Briefing, PRD, arquitetura, contrato visual e status do projeto foram registrados.

O baseline foi iniciado localmente na porta 3010 e respondeu HTTP 200. O health check retorna `ok: true`; Node/npm/Git aparecem como avisos apenas porque o sandbox bloqueia subprocessos filhos.

### Validation

`npm run health:check` passou. `npm run lint` passou com 85 avisos legados e 0 erros. `npm run typecheck` passou. `npm test` passou sem testes no baseline antigo. `npm run build` passou com a configuração temporária de 1 worker e typecheck externo; o build padrão é bloqueado por `spawn EPERM` do sandbox, limitação registrada sem ser gravada em `next.config.ts`.
